# =============================================================================
# UMaT Alumni – Nginx Production Gateway (domain mode)
# =============================================================================
# Routing model:
#
#   alumat.umat.edu.gh           → frontend-member:3000  (member portal)
#   admin-alumat.umat.edu.gh     → frontend-admin:3000   (admin portal)
#   alumat-api.umat.edu.gh       → member-api:8080
#   admin-alumat-api.umat.edu.gh → admin-api:8080
#   <server-ip> (default)        → 301 → alumat.umat.edu.gh
#
# HTTPS upgrade: run  ./deploy.sh --ssl  then add SSL blocks and restart nginx.
# =============================================================================

events {
    worker_connections 2048;
    use epoll;
    multi_accept on;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    # ── Logging ────────────────────────────────────────────────────────────────
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    access_log /var/log/nginx/access.log main;
    error_log  /var/log/nginx/error.log  warn;

    # ── Performance ────────────────────────────────────────────────────────────
    sendfile           on;
    tcp_nopush         on;
    tcp_nodelay        on;
    keepalive_timeout  65;
    types_hash_max_size 2048;
    server_tokens      off;

    client_max_body_size 50M;

    # ── Compression ────────────────────────────────────────────────────────────
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied    any;
    gzip_comp_level 6;
    gzip_types
        text/plain text/css text/xml text/javascript
        application/json application/javascript application/xml
        application/rss+xml font/truetype font/opentype image/svg+xml;

    # ── Global proxy settings ──────────────────────────────────────────────────
    proxy_http_version    1.1;
    proxy_set_header      Host              $host;
    proxy_set_header      X-Real-IP         $remote_addr;
    proxy_set_header      X-Forwarded-For   $proxy_add_x_forwarded_for;
    proxy_set_header      X-Forwarded-Proto $scheme;
    proxy_set_header      Upgrade           $http_upgrade;
    proxy_set_header      Connection        keep-alive;
    proxy_read_timeout    120s;
    proxy_connect_timeout 10s;
    proxy_send_timeout    120s;
    proxy_buffer_size     4k;
    proxy_buffers         8 4k;

    # ── Security headers ───────────────────────────────────────────────────────
    add_header X-Frame-Options        "SAMEORIGIN"                       always;
    add_header X-Content-Type-Options "nosniff"                          always;
    add_header X-XSS-Protection       "1; mode=block"                    always;
    add_header Referrer-Policy        "strict-origin-when-cross-origin"  always;
    add_header Permissions-Policy     "geolocation=(), microphone=()"    always;

    # ── Docker embedded DNS ────────────────────────────────────────────────────
    resolver 127.0.0.11 valid=30s ipv6=off;
    resolver_timeout 5s;

    # ==========================================================================
    # alumat-api.umat.edu.gh  –  Member API
    # ==========================================================================
    server {
        listen 80;
        server_name alumat-api.umat.edu.gh;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl http2;
        server_name alumat-api.umat.edu.gh;

        ssl_certificate     /etc/letsencrypt/live/alumat.umat.edu.gh/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/alumat.umat.edu.gh/privkey.pem;
        include /etc/nginx/ssl-params.conf;

        location / {
            set $upstream http://alumni-member-api:8080;
            proxy_pass $upstream;
        }
    }

    # ==========================================================================
    # admin-alumat-api.umat.edu.gh  –  Admin API
    # ==========================================================================
    server {
        listen 80;
        server_name admin-alumat-api.umat.edu.gh;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl http2;
        server_name admin-alumat-api.umat.edu.gh;

        ssl_certificate     /etc/letsencrypt/live/alumat.umat.edu.gh/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/alumat.umat.edu.gh/privkey.pem;
        include /etc/nginx/ssl-params.conf;

        location / {
            set $upstream http://alumni-admin-api:8080;
            proxy_pass $upstream;
        }
    }

    # ==========================================================================
    # admin-alumat.umat.edu.gh  –  Admin Portal
    # ==========================================================================
    server {
        listen 80;
        server_name admin-alumat.umat.edu.gh;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl http2;
        server_name admin-alumat.umat.edu.gh;

        ssl_certificate     /etc/letsencrypt/live/alumat.umat.edu.gh/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/alumat.umat.edu.gh/privkey.pem;
        include /etc/nginx/ssl-params.conf;

        location / {
            set $upstream http://alumni-frontend-admin:3000;
            proxy_pass $upstream;
        }
    }

    # ==========================================================================
    # alumat.umat.edu.gh  –  Member Portal (root domain)
    # ==========================================================================
    server {
        listen 80;
        server_name alumat.umat.edu.gh www.alumat.umat.edu.gh;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location = /health {
            access_log off;
            add_header Content-Type text/plain;
            return 200 "OK\n";
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl http2;
        server_name alumat.umat.edu.gh www.alumat.umat.edu.gh;

        ssl_certificate     /etc/letsencrypt/live/alumat.umat.edu.gh/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/alumat.umat.edu.gh/privkey.pem;
        include /etc/nginx/ssl-params.conf;

        location = /health {
            access_log off;
            add_header Content-Type text/plain;
            return 200 "OK\n";
        }

        location / {
            set $upstream http://alumni-frontend-member:3000;
            proxy_pass $upstream;
        }
    }

    # ==========================================================================
    # cdn.alumat.umat.edu.gh  –  MinIO CDN (public file serving)
    # ==========================================================================
    server {
        listen 80;
        server_name cdn-alumat.umat.edu.gh;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    server {
        listen 443 ssl http2;
        server_name cdn.alumat.umat.edu.gh;

        ssl_certificate     /etc/letsencrypt/live/alumat.umat.edu.gh/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/alumat.umat.edu.gh/privkey.pem;
        include /etc/nginx/ssl-params.conf;

        location / {
            set $upstream http://alumni-minio:9000;
            proxy_pass $upstream;
            proxy_set_header  Host $http_host;
            # Remove CORS headers from MinIO so nginx controls them
            proxy_hide_header Access-Control-Allow-Origin;
            add_header        Access-Control-Allow-Origin "*" always;
        }
    }

    # ==========================================================================
    # Default – redirect bare IP to canonical domain
    # ==========================================================================
    server {
        listen 80 default_server;

        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://alumat.umat.edu.gh$request_uri;
        }
    }
}
